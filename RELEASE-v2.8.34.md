# AgoraIn v2.8.34

> 课堂打卡与集控平台 · 双模式桌面客户端 + 集控服务器 + 移动端
>
> 📚 在线文档：https://doc.615mc.cn

---

## ✨ 新增

### 双模式窗口（一个客户端，两种形态）

- **大屏模式**（默认）：课堂投影打卡面板
  - 左侧任务树 + 右侧多标签页，支持新建 / 切换 / 双击重命名，工作区状态自动保存恢复
  - 学生按钮网格一键打卡（行列可配置，默认 6×6），右键取消打卡
  - 实时打卡排名：第 1/2/3 名金 / 银 / 铜高亮
  - 在线模式：连接集控服务器实时同步，顶栏在线 / 离线指示灯
  - 二维码签到任务：一键创建签到，支持签到密码、教室、科目，学生扫码自动打卡
- **控制模式**：教学管理员的控制中心
  - 统计条：总设备 / 总任务 / 在线设备实时数量
  - 左侧导航：划课 / 设备列表 / 任务中心 / 集控平台列表

### 课时划消与排课系统

- 学生管理：添加 / 删除学生、设置初始课时，剩余课时蓝色徽章展示
- 课时划消：划消 / 增加课时 + 备注，流水记录红扣绿增
- 月历排课：上课 / 下课时间必填（支持跨天），今日"今"徽章、选中蓝框、重复排课拦截
- 不排课日：一键设置 / 恢复，自动清空当日排课，周末默认提示
- 复制排课：把某日排课复制到多个日期，可跳过不排课日
- **自动扣减课时**：按实际时长 × 每小时课时消耗自动划消，SlotKey 幂等去重

### 集控平台

- 控制台：登录、仪表盘、设备、任务、考勤、用户管理
- 多用户 + JWT 认证、加盐 SHA256 密码哈希、设备分配

## 🎨 UI / 体验

- 模式切换下拉框（白色半透明样式）
- 现代化圆角工艺：输入框 / 选项卡 / 右键菜单 / DWM 窗口圆角
- SVG 圆形应用图标
- 全局异常兜底：未处理异常写入日志，避免闪退后无从排查

## 🛠 修复

- 日历选中蓝框跟随日期
- 大屏模式取消打卡失效
- 控制模式显示错误模式名
- 白字继承链问题（下拉框 / 菜单文字颜色）

---

## 📦 资源下载

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| Windows x64 | `AgoraIn-Setup-v2.8.34.exe` | 桌面客户端安装包（自包含 .NET 运行时，免装依赖） |
| Windows x64 | `AgoraIn-Win-x64-Client.zip` | 免安装便携版 |
| Windows x64 | `AgoraIn-Win-x64-Server.zip` | 集控服务器 |
| Linux x64 | `AgoraIn-Linux-x64-Server.zip` | 集控服务器（Linux 部署） |
| macOS x64 | `AgoraIn-MacOS-x64-Server.zip` | 集控服务器（macOS 部署） |

> 服务器默认端口 **5250**，首次启动自动生成连接密码并写入 `config.json`。
> 移动端（Android / iOS）请从应用商店或企业分发渠道获取。

## 🚀 快速开始

```powershell
# 1. 解压 / 安装客户端，双击 AgoraIn.exe
# 2. 启动集控服务器（可选，离线模式无需）
dotnet run --project Server/CheckIn.Server.csproj

# 3. 客户端「远程 → 远程服务器设置」填入服务器 IP、端口 5250 与密码
```

## 🧭 使用指南

1. **大屏打卡**：任务树选择任务 → 点击学生按钮打卡 → 右侧排名实时更新
2. **课时划消**：控制模式 → 划课 → 选中学生 → 划消 / 增加课时
3. **排课**：月历选日期 → 添加学生并填写上课 / 下课时间（必填）→ 开启自动扣减课时
4. **集控平台**：控制模式 → 集控平台列表 → 打开控制台 → 登录管理设备与用户

## 🔒 安全说明

- 服务器未配置密码时自动生成随机密码，不落明文
- 管理员密码 SHA256 加盐哈希存储
- 私钥使用 Windows DPAPI 加密存储

## 📄 数据文件

- `config.json`：全局配置（学校 / 班级 / 课程 / 网格行列 / 服务器信息）
- `workspace.json`：标签页工作区状态
- `data/classhours.json`：课时划消与排课数据
- 服务器 `config.json` + SQLite 数据库：设备 / 任务 / 考勤 / 用户

## 🙏 反馈

- 问题反馈：[Issues](https://github.com/your-repo/check-in-net/issues)
- 在线文档：https://doc.615mc.cn
